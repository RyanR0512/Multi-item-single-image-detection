[project]
name = "nanobanana"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "streamlit",
    "torch",
    "torchvision",
    "timm",
    "numpy",
    "Pillow",
    "requests",
    "ultralytics",
]
