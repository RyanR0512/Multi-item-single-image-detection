[project]
name = "fluxdetector"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "streamlit",
    "torch==2.9.0",
    "torchvision",
    "timm",
    "numpy",
    "Pillow",
    "requests",
    "ultralytics",
]
