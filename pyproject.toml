[project]
name = "fluxdetector"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "streamlit",
    "torch>=2.6.0",
    "torchvision>=0.20.0",
    "timm",
    "numpy",
    "Pillow",
    "requests",
    "ultralytics",
]
